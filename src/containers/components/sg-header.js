import React, {Component} from 'react'

export default class SgHeader extends Component{

  render() {
    return(
      <header className="header__wrap">
        This is header
      </header>
    );
  }
}